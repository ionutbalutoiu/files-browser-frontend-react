# API Configuration
# Base URL for API endpoints (default: same origin)
VITE_API_ORIGIN=

# Base URL for file listings (default: same origin)
VITE_FILES_ORIGIN=

# Base URL for share links (recommended to set this)
VITE_SHARE_ORIGIN=

# Share URL template (default: /s/{shareId})
VITE_SHARE_URL_TEMPLATE=/s/{shareId}
